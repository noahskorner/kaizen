import './globals.css';
import { TextInput } from '@kaizen/ui';

export default function App() {
  return (
    <div className="h-screen w-screen bg-slate-900">
      <TextInput id={''} label={''} value={''} onChange={() => {}} />
    </div>
  );
}
